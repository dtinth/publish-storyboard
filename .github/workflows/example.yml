on:
  push:
    branches:
      - main
jobs:
  example:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - run: npx 'github:dtinth/publish-storyboard#main' --input=fixture
